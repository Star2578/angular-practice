import { RecursiveWarningDirective } from './recursive-warning.directive';

describe('RecursiveWarningDirective', () => {
  it('should create an instance', () => {
    const directive = new RecursiveWarningDirective();
    expect(directive).toBeTruthy();
  });
});
